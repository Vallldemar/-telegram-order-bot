services:
  - type: web
    name: telegram-order-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python order_bot.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.13
